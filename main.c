//this is a new file in first commit
#include <stdio.h>

int main() {
    int x, y;
    printf("Hello git\n");

    printf("ins first num: ");
    scanf("%d", &x);
    printf("ins seccond num: ");
    scanf("%d", &y);

    printf("%d + %d = %d\n", x, y, x + y);
}